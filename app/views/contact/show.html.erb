<p id="notice"><%= notice %></p>

<article id="main">
  <header>
    <h2>Contact Requests</h2>
  </header>
  <section class="wrapper style5">
    <div class="inner">
    <section>
      <table>
        <thead>
          <tr>
            <th>Name</th>
            <th>Email</th>
            <th>Phone</th>
            <th>Bar</th>
            <th>Competition</th>
            <th>Message</th>
          </tr>
        </thead>

        <tbody>
          <% @contacts.each do |contact| %>
            <tr>
              <td><%= contact.name %></td>
              <td><%= contact.email %></td>
              <td><%= contact.phone %></td>
              <td><%= contact.bar %></td>
              <td><%= contact.competition %></td>
              <td><%= contact.message %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </section>
</article>
